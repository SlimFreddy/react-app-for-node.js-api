import { FC } from "react";
import RouterView from "./router/RouterView";
import "./App.scss";
const App: FC = () => {
  return (
    <div className="App">
      <RouterView />
    </div>
  );
};

export default App;
